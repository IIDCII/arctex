# .env.template
# Rename this file to .env and replace values as needed

BUILD_TYPE=Debug

# Your Linux user and group IDs (run `id -u` and `id -g` in terminal)
USER_ID=1000
GROUP_ID=1000

# Username to create inside the container (should match your system username ideally)
USERNAME=johnsmith

# Path to your Neovim config (should contain a `lua/` directory if customizing LazyVim)
NVIM_CONFIG_PATH=/home/johnsmith/.config/nvim

# (Optional) Absolute path to the project you want to mount into the container
# If not set, the container will default to the parent directory of this repo
PROJECT_DIR=/home/johnsmith/path/to/project
